import { DesignerLayout } from '@/components/designer/DesignerLayout';
import { DailyTasksContent } from '@/pages/shared/DailyTasksPage';

export default function DesignerDailyTasks() {
  return (
    <DesignerLayout>
      <DailyTasksContent />
    </DesignerLayout>
  );
}
